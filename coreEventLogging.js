const SCACommunicator = require(".");

module.exports = SCACommunicatorInstance => {
  if (!SCACommunicatorInstance) throw new Error('You are missing an instantiated SCACommunicator instance');
  this = SCACommunicatorInstance;

  
};